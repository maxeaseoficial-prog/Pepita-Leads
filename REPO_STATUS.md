# Status do repositório

O projeto está preparado para o repositório GitHub:

`maxeaseoficial-prog/Pepita`

O ambiente desta conversa não possui uma integração autenticada de escrita no GitHub.
Por isso o repositório remoto não foi criado automaticamente.

Use:

```bash
./scripts/publish-github.sh Pepita maxeaseoficial-prog
```

em uma máquina com `gh auth login`.
